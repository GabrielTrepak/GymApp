// Placeholder simples para telas que ainda vamos construir
// (Planos de dieta do personal, Dieta e Progresso do cliente).
export default function PaginaEmBranco({ titulo }) {
  return (
    <div>
      <h1 className="text-xl">{titulo}</h1>
      <p className="text-muted">Em construção.</p>
    </div>
  )
}
